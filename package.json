{
  "name": "manta-travesia-jocay",
  "version": "1.0.0",
  "description": "Videojuego móvil sobre Manta y la cultura manteña",
  "scripts": {
    "build:android": "npm run cap:sync && cd android && gradlew assembleDebug",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android"
  },
  "capacitor": {
    "platforms": [
      "@capacitor/android"
    ]
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
